Мы используем Zod для валидации и типизации данных.

Основные данные определены в `constants.ts`.

Все остальные типы выводятся из них.

В `index.ts` собираются все типы и функции.

В `validation.ts` собираются все валидаторы.


1. User
2. Family
3. Transaction
4. FamilyTransaction
5. Event
